今天尝试了一下新的 plan 工作流

1. 在 session1 提出问题，不停补充细节，让 Claude 给出方案
2. 根据 session1 里的目标，确认是否完成，复制出来给 session2（只复制目标），让 session 2 重新给一个计划
3. 在 session3 里让 Claude review 这两个计划，critically，然后让它给出一个更优的方案（不止是简单的合并）

